import { Todo } from './todo.model';

const mockTodos: Todo[] = [
  new Todo(1, 'Learn TypeScript'),
  new Todo(2, 'Install Angular CLI'),
  new Todo(3, 'Make TODOP project'),
  new Todo(4, 'Learn Routing and DI'),
  new Todo(5, 'Employ HTTP Client')
];

export default mockTodos;
